# Dashboard Project

## Skills
When looking up FRED series codes, always read `.claude/skills/fred-lookup/SKILL.md` before proceeding.

## Project Structure
- Frontend: React + TypeScript (Vite), runs on localhost:5173
- Backend: Node.js/Express
- Database: SQLite (fred_data.db)
- Environment: .env at project root (FRED_API_KEY)
